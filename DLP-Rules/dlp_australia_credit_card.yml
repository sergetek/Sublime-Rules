name: "DLP: Australia Credit Card Number - Any Direction"
description: "Detects messages containing credit card numbers."
type: "rule"
severity: "high"
source: |
  any(attachments,
          (
            .file_extension in~ $file_extensions_common_archives
            or .file_extension in~ ('.csv', '.txt', '.xlsx', '.xls')
          )
          and any(file.explode(.),
                  // Credit card: 13-19 digits with optional spaces/hyphens
                  regex.contains(.scan.ocr.raw, '\b(?:\d{4}[\s\-]?){3}\d{1,7}\b')
          )
  )

detection_methods:
-   "File analysis"
-   "Optical Character Recognition"
